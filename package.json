{
  "name": "exercicio_1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "scripts": {
    "test": "mocha \"test/**/*.js\"",
    "test:ci": "mkdir -p reports && mocha \"test/**/*.js\" --reporter mocha-junit-reporter --reporter-options mochaFile=./reports/test-results.xml && mocha \"test/**/*.js\" --reporter mochawesome --reporter-options reportDir=./reports,reportFilename=mochawesome-report,quiet=true"
  },
  "devDependencies": {
    "mocha": "^11.7.6",
    "mocha-junit-reporter": "^2.2.1",
    "mochawesome": "^7.1.4"
  }
}
